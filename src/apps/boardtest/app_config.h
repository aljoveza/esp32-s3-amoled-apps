#pragma once
#include "board_config.h"

// Board self-test — app configuration.
#define BT_FRAME_MS        60    // refresh rate of the live values
#define BT_FOLLOW_DEVICE   1     // rotate the report with the device
